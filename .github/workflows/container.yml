name: Container
on: push

jobs: 
  docker-steps:
    runs-on: ubuntu-latest
    container:
      image: node:okteto/node
    steps:
      - name: log node version
        run: |
          node -v
          cat /etc/os-version
      